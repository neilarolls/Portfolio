window.onload = (e) => {

    const titleWrapper = document.getElementById("code-transporter-header");
    const contentWrapper = document.getElementById("code-transporter-content");
    const contentWrapperHeight = (contentWrapper.offsetHeight);

};